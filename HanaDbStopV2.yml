---
- hosts: "{{ host }}"
  user: root
  become_user: "{{ dbadm }}"
  gather_facts: yes
  tasks:
   - template:
      src: HanaDbStopV2.j2
      dest: ~/HanaDbStopV2.sh
      mode: 0755
   - script: ~/HanaDbStopV2.sh
     register: cmd2
   - debug: msg="{{ cmd2.stdout_lines }}"
...
