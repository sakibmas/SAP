---
- hosts: "{{ host }}"
  user: root
  become_user: "{{ dbadm }}"
  gather_facts: yes
  tasks:
   - template:
      src: HanaDbStopV2.j2
      dest: ~/HanaDbStopV2.sh
      mode: 0755
   - command: ~/HanaDbStopV2.sh
     register: cmd1
   - debug: msg="{{ cmd1.stdout_lines }}"
...
