
pipeline {
    agent {label 'clab'};
    stages {
        stage('Stop the HANA database') {
            steps {
                sh 'ansible-playbook HanaDbStop.yml -e "host=$hostname"'
            }
        }
    }
}
