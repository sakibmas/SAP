
pipeline {
    agent any;
    stages {
        stage('Stop the HANA database') {
            steps {
                sh 'whoami'
                sh 'echo ansible-playbook HanaDbStop.yml -e "host=$hostname"'
            }
        }
    }
}
