
pipeline {
    agent any;
    stages {
        stage('Print the variable') {
            steps {
                // in groovy
                echo "${env.hostname}"
                
                //in shell
                sh 'echo $hostname'
                
                // in groovy script 
                script {
                    print env.hostname
                }
            }
        }
    }
}
