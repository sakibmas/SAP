pipeline {
    agent {label 'clab'};
    stages {
        stage('Stop the HANA database') {
            steps {
                sh 'ansible-playbook SAP/HanaDbStop.yml -e "host=$hostname"'
            }
        }
    }
}
