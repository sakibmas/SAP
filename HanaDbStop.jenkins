
pipeline {
    agent any;
    stages {
        stage('Print the variable') {
            steps {
                sh 'echo ansible-playbook HanaDbStop.yml -e "host=$hostname"'
            }
        }
    }
}
