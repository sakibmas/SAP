
pipeline {
    agent any;
    stages {
        stage('Print the variable') {
            steps {
                sh 'ansible-playbook HanaDbStop.yml -e "host=$hostname"'
            }
        }
    }
}
